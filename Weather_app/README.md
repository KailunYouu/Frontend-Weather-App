How to run: open terminal, "cd Weather_app", then "npm install", last run "npm run dev"

I used REACT for the frontend of the webpage
openWeatherMap API to give location(city) and retrieve temperature about that location.
